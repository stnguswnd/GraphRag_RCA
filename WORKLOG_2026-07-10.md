# 작업 로그 — 학술 논문(PDF) 활용 + fab 검증 파이프라인 확장

> 날짜: 2026-07-10 · 브랜치: `gus`
> 커밋: `a0473e6`(본작업) → `8c73d9c`(논문 PDF 추가)
> 한 줄 요약: **웨이퍼 불량 RCA 지식그래프에 학술 논문(PDF)의 "패턴→원인" 지식을 흡수하고, KG 가설을 fab 텔레메트리로 실제 검증하는 데까지 파이프라인을 확장했다.**

---

## 0. TL;DR (무엇이 달라졌나)

| 영역 | 이전 | 이후 |
|---|---|---|
| 입력 문서 | `.txt` troubleshooting 7편 | + **논문 PDF 3편** (pdfplumber 컬럼 추출) |
| DefectPattern | 3종 (Center/Scratch/Edge-Ring) | **8종** (WM-811K 표준) |
| 관계 종류 | ARISES_IN, OCCURS_IN, CAUSED_BY, INVOLVES_PARAMETER, PART_OF | + **`ATTRIBUTED_TO`** (DefectPattern→Cause, 문헌) |
| 논문 지식 | 스키마에 안 맞아 **전량 폐기** | 패턴→원인 표를 백본에 **흡수** |
| fab 검증 | 설계만 존재, 코드/데이터 없음 | **목업 DB + 검증 코드로 실제 판정** |
| 질의 출력 | fab 가설만 | fab 가설 + **문헌 기반 후보 원인** |

---

## 1. 배경과 문제 제기

### 1.1 시작점
사용자가 `data/docs/`에 학술 논문 PDF 3편을 넣고, "이것도 청킹되게 코드 추가해달라"고 요청.
- `ref56_shin2025` — *A framework for detecting unknown defect patterns on wafer bin maps* (WM-811K 패턴별 원인 표 포함)
- `ref57_kang2024` — *Similarity searching for wafer bin maps* (유사도 검색 기법)
- `ref58_qian2024` — *Deep imbalanced separation network* (딥러닝 검출망)

### 1.2 핵심 통찰 — 문제는 두 겹이었다
논문을 실제로 추출해보니(초기 `pypdf` 버전, ref56 192청크 테스트):
- 토큰 518,064개를 쓰고도 유효 청크는 **3/192개**뿐, 나머지는 초록·수식·참고문헌.
- 살아남은 것조차 노이즈(`humans`, `misclassification` 등).

원인을 c27 청크에서 추적한 결과, **두 개의 다른 문제**임이 드러남:
1. **표 파싱 실패** — 논문은 2단 편집 + 테두리 없는 표라, `pypdf`가 좌/우 칸을 뒤섞어 "패턴 | 원인" 행이 깨짐.
2. **스키마 미스매치(더 근본)** — 논문 표는 `DefectPattern → 원인`을 **직접** 주는데, 기존 스키마엔 그 관계가 없었다. Cause는 오직 `FailureMode -CAUSED_BY-> Cause` 경로로만 도달 가능. 그래서 LLM이 `Center → irregular_rf_operation`을 **정확히 읽어도** `validate_kg`가 통째로 버렸다.

> 즉 "관련 없다고 버린 게 아쉽다"는 사용자의 직관은 옳았고, 원인은 표 파싱보다 **스키마에 자리가 없어서**였다.

---

## 2. 설계 결정

논문 지식을 담는 세 가지 방안을 두고 사용자와 논의:

| 방안 | 내용 | 판단 |
|---|---|---|
| ① 백본 흡수 | DefectPattern 확장 + `DefectPattern→Cause` 관계 추가 | **채택** |
| ② 별도 문헌 레이어 | `LitClaim`/`LitEntity` 별도 라벨 | 기각 (도메인 이중화, 그래프 가치 얇음) |
| ③ RAG로만 | 그래프화 안 함, 검색 컨텍스트로만 | 기각 (지식 그래프화 목적과 안 맞음) |

**채택 근거:** 논문의 defect→cause 표는 RCA 도메인과 정확히 같은 모양이라, 별도 레이어로 쪼개기보다 **백본에 흡수**하는 게 중복이 없고 질의에서 바로 쓰인다. 단, `source='literature'` 태그로 fab 검증 백본과 구분.

---

## 3. 구현 상세

### 3.1 PDF 추출 — `2_load_txt.py` (pypdf → pdfplumber 컬럼 인식)

`pdfplumber`가 **글자 좌표(x0, x1, top)**를 주는 걸 이용해 2단 레이아웃을 복원.

```python
X_TOLERANCE = 1.0        # 글자 간격이 이 이상이면 공백 삽입 (붙은 단어 복원)
STRADDLE_RATIO = 0.05    # 가운데를 걸치는 단어 비율이 이 미만이면 2단으로 판단

def _extract_page_text(page):
    mid = page.width / 2
    words = page.extract_words(...)
    straddle = sum(1 for w in words if w["x0"] < mid < w["x1"])
    left  = [w for w in words if w["x1"] <= mid]
    right = [w for w in words if w["x0"] >= mid]
    two_col = straddle < STRADDLE_RATIO*len(words) and len(left)>10 and len(right)>10
    if two_col:
        # 좌 컬럼 전체 → 우 컬럼 전체 순으로 읽는다
        return crop(left).extract_text(x_tolerance=X_TOLERANCE) + "\n\n" + crop(right)...
    return page.extract_text(x_tolerance=X_TOLERANCE)
```

**컬럼 판정 원리:** 진짜 2단은 가운데 빈 골(gutter)이 있어 그 선을 넘는 단어가 거의 없다(straddle≈0). 전폭 제목/표는 넘는 단어가 많다(straddle≈줄 수). ref56에서 제목 페이지(0쪽, straddle 26)는 1단, 본문 2단 페이지들은 2단으로 정확히 갈렸다.

**효과:** 깨져 있던 defect 원인 표가 행 구조 그대로 복원됨.
```
Center     Defects concentrated ...   Irregular radio frequency (RF) operation ... or unusual liquid flow
Edge-Ring  Defects located along ...  Anomalous temperature regulation during rapid thermal process (RTP)...
```

**한계(정직):** 가운데 반으로 자르는 휴리스틱이라 3단·비대칭·전폭 표가 낀 2단 페이지엔 취약. 표준 2단 논문엔 잘 통함.

### 3.2 스키마 변경

- **DefectPattern 3→8종** (`data/seeds/defect_patterns.json` + `5_..`의 `DefectPatternId` Literal + 프롬프트, `assert_enums_match_seeds`가 일치 강제): Center, Donut, Edge-Loc, Edge-Ring, Loc, Near-Full, Random, Scratch. WM-811K 표준이라 VLM 분류기와도 정렬.
- **새 관계 `ATTRIBUTED_TO`** (`DefectPattern → Cause`, 속성 `source='literature'`): 논문 표의 패턴→원인 직결. `Cause`는 백본과 **같은 라벨 공유**(이중화 방지).

```
이전:  DefectPattern ─ARISES_IN─> ProcessStep <─OCCURS_IN─ FailureMode ─CAUSED_BY─> Cause ─INVOLVES_PARAMETER─> Parameter
이후:  + DefectPattern ─ATTRIBUTED_TO(문헌)──────────────────────────────────────> Cause  (같은 Cause 노드)
```

### 3.3 `5b_extract_pattern_causes.py` (신규) — 논문 패턴→원인 타깃 추출

- **입력:** pdf 청크만. `5_..`는 txt 백본만 담당하도록 분리(논문 노이즈 회피).
- **prefilter:** 패턴 이름이 실제 등장하는 청크만 LLM에 보냄 (509 → 대상 70청크).
- **추출:** `(DefectPattern, Cause)` 쌍만. Cause는 자유 텍스트(snake_case).
- **노이즈 필터 `cause_noise_reason()`:**
  1. 동어반복 — cause가 사실 패턴 이름 (`Center←Center`)
  2. 방법론/분류 키워드 블록리스트 (`misclassification`, `training data`, `weighting`, `voting`, `combining`, `location aspect`, `partial ring` …)
  3. `(pattern, cause_id)` 중복 제거
- **적재:** `MERGE (dp)-[:ATTRIBUTED_TO {source:'literature', ...}]->(cause)` + `(chunk)-[:MENTIONS]->(cause)`

### 3.4 `6_ask_graphrag.py` — 문헌 후보 원인 출력 + 공정 정합성 가드

두 쿼리를 패턴마다 각각 실행:
- **`HYPOTHESIS_QUERY`** (fab 가설, 엄격): 완전경로 `DefectPattern→ProcessStep→FailureMode→Cause→Parameter`를 전부 MATCH.
- **`LITERATURE_QUERY`** (문헌 후보, 느슨): `DefectPattern-[:ATTRIBUTED_TO]->Cause`, Parameter는 OPTIONAL.

두 결과를 별 섹션으로 분리 출력 → 검증 가능/불가를 섞지 않음.

### 3.5 fab 목업 + 검증 (서브 에이전트로 작성)

`Parameter`가 fab `telemetry.param`과 연결되는 join key인데, **실제 fab 데이터도 조회 코드도 없었음**(설계만 존재). 그래서 목업을 구축:

- **`data/fab/fab_model.yaml`** — 20개 파라미터의 정상범위(ground truth).
- **`data/fab/generate_fab.py`** — `fab.db`(SQLite) 생성. `random.seed(42)`. 표 4종(lot_history 60·telemetry 1100·alarm 5·maintenance 4). telemetry.param은 20개 Parameter.id와 **정확히 일치**.
- **KG 가설에 맞춘 이상 시나리오 4건 심음:** LOT-0001/DEPO/gas_flow↑, LOT-0003/ETCH/etch_rate↑, LOT-0006/CMP/down_force↑ & slurry_flow↓.
- **`7_verify.py`** — `verify(lot, step, param, direction)`: lot_history↔telemetry 조인 후 정상범위와 대조해 `정상/이상·가설지지` 판정. Neo4j에서 가설을 뽑아 end-to-end 실행.

---

## 4. 실행 & 검증 결과

파이프라인 전체 재실행: `0_reset → 4 → 5(txt) → 5b(pdf) → 6 → 7_verify`.

### 4.1 적재 결과 (Neo4j)
```
노드:  DefectPattern 8 · Cause 43 · FailureMode 17 · Parameter 20 · Chunk 509 · Document 10 · ProcessStep 6
관계:  ATTRIBUTED_TO 21 · CAUSED_BY 24 · INVOLVES_PARAMETER 20 · OCCURS_IN 18 · ARISES_IN 5 · MENTIONS 78 ...
```

### 4.2 논문 흡수 품질
- 5b 추출: 70청크 → 48쌍 → **노이즈 필터 후 21쌍** (8패턴 전부).
- 논문별: **ref56=금맥**(진짜 원인 다수), ref57=절반 노이즈, ref58=0건(딥러닝 방법론, LLM이 올바르게 아무것도 안 뽑음).
- 최종 예: `Center←RF이상/액체흐름`, `Edge-Ring←RTP온도이상·cold process`, `Loc←진공압력차·slit valve 누설`, `Near-Full←PR rupture·전자과충전`, `Donut←photoresist 잔류물`.

### 4.3 fab 검증 (실제 판정)
심은 이상 4건이 전부 **가설 지지**, 나머지는 **정상/기각**:
```
Center / LOT-0001   ◎ gas_flow   이상 (136.75 vs 정상 80~120) → 지지
Edge-Ring / LOT-0003 ◎ etch_rate 이상 (538.6 vs 정상 480~520) → 지지
Scratch / LOT-0006  ◎ down_force 이상 (5.72 vs 정상 3~5) → 지지
                    ◎ slurry_flow 이상 (105.6 vs 정상 150~250, 낮음) → 지지
```
이전엔 "etch_rate 확인하라"는 **지시문**만 나왔는데, 이제 그 값을 실제 조회해 **판정**까지 한다.

---

## 5. 설계 판단과 근거 (정직한 기록)

이번 작업에서 내린 주요 판단들:

1. **5b를 5번과 분리** — 입력(txt vs pdf), 추출 대상(전체 백본 vs 패턴→원인만), 비용 전략(전량 vs prefilter)이 달라서. 코드 중복은 있으나 저장소 스타일과 맞고, 합치면 if 분기 범벅이 됨.

2. **fab "검증"은 목업임을 명시** — 실제 fab 데이터가 없어, 검증이 되는 걸 보이려고 KG 가설에 맞춰 이상을 **심었다**. "데이터만 있으면 검증까지 돈다"의 증명이지 실측이 아님.

3. **cross-step 버그 → 쿼리 가드로 해결** (`WHERE s.id IN param.steps`). 검증이 `Scratch→CMP→megasonic_power`가 잘못됐음을 드러냄(`megasonic_power`는 CLEAN 변수). 뿌리는 STATUS **P3**(FailureMode `wafer_surface_damage`가 CMP·CLEAN에 병합). "검증 변수는 그 공정 소속이어야 한다"는 정합성 불변식을 쿼리에 넣어 출력 단계에서 차단(band-aid 아닌 옳은 자리). 근본 해결(FailureMode 공정 스코프)은 미결.

4. **doc_A 3→8 유추 확장은 안 함** — 논문이 5개 신규 패턴에 대해 대는 원인이 우리 6공정 밖(anneal/ion-implant)을 가리켜, 공정 매핑을 넣으면 **근거 없는 값을 지어내 백본을 오염**시킴. 정직한 "가설 없음"이 가짜 완결성보다 낫다. 라벨만 "커버리지 차이"로 정리. → 검증 가능성을 원하면 **문헌 Cause→Parameter 매핑**(출처 태그 유지)이 더 정직한 길.

---

## 6. 남은 과제 / 한계

- **문헌 Cause가 Parameter에 미연결** → 문헌 후보 원인은 아직 "정성적 단서"에 머묾(검증 경로 미완). 20개 고정 변수로 resolve하면 검증까지 이어짐.
- **P3 근본 해결 미결** — `FailureMode.id`를 `{STEP}:{name}`로 스코프하면 병합 자체가 사라짐. 쿼리 가드는 출력만 방어.
- **fab 데이터는 목업** — 실제 telemetry/조회 파이프라인은 별도 구축 필요.
- **ref57 잔여 노이즈** — `linear patterns` 등 패턴 묘사성 원인 일부 잔존.
- **VLM 입력 미구현** — 6번은 여전히 패턴 id 직접 입력.

---

## 7. 파일 변경 목록

### 신규
- `5b_extract_pattern_causes.py` — 논문 패턴→원인 타깃 추출
- `7_verify.py` — KG 가설 fab 검증
- `data/fab/generate_fab.py` — 목업 fab.db 생성기
- `data/fab/fab_model.yaml` — 파라미터 정상범위
- `extraction_flow.md` — 추출/적재 흐름 설명
- `WORKLOG_2026-07-10.md` — 이 문서

### 수정
- `2_load_txt.py` — pdf 컬럼 인식 추출
- `5_build_kg_from_chunks.py` — DefectPattern 8종, txt만 처리, 프롬프트 정정
- `6_ask_graphrag.py` — 문헌 후보 원인 + 공정 정합성 가드
- `data/seeds/defect_patterns.json` — 8종, `_coverage_note`
- `README.md` / `STATUS.md` / `schema.md` — 스키마·흐름 반영

### 삭제
- `6_ask_graphrag_backup.py` — 죽은 백업

### gitignore (재생성 가능 생성물)
- `outputs/*.jsonl`, `data/fab/fab.db`

### 커밋
- `a0473e6` 논문(PDF) 활용 + fab 검증까지 파이프라인 확장
- `8c73d9c` 논문 원본 PDF 3편 추가

---

## 부록 — 전체 실행 순서

```bash
python 0_reset.py                      # 스키마 변경(3→8) 후 필수
python 2_load_txt.py                   # txt + pdf(컬럼 인식) -> parsed_docs.jsonl
python 3_split.py                      #                       -> chunks.jsonl
python 4_ingest_chunks_to_neo4j.py     # 시드 앵커(8종) + Chunk 적재
python 5_build_kg_from_chunks.py       # 통합 추출 + 원인 표준화 (※3차 재설계로 5b/5c 흡수)
python 6_ask_graphrag.py               # 가설 + 문헌 후보 원인
python data/fab/generate_fab.py        # 목업 fab.db 생성
python 7_verify.py                     # KG 가설을 fab 텔레메트리로 검증
```
